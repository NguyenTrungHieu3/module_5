/**
 * Bài 3: Thực hành khởi tạo dự án với Vite
 * Mục tiêu: Tự tay thiết lập và chạy một dự án React thực tế.
 * 
 * Yêu cầu (thao tác ở Terminal):
 * 1. Mở Terminal và đảm bảo bạn đang ở thư mục hiện tại (`thuc_hanh`).
 * 2. Chạy lệnh tạo một dự án Vite mới với tên `my-second-vite-app` (sử dụng template react).
 * 3. Di chuyển vào thư mục dự án vừa tạo, tiến hành cài đặt các thư viện.
 * 4. Khởi động server ảo.
 * 5. Mở file `src/App.jsx` trong dự án mới tạo, xóa toàn bộ giao diện mặc định
 *    trong thẻ `return (...)` và thay bằng thẻ:
 *    <h1>Chào mừng tôi đến với thế giới React!</h1>
 * 6. Lưu lại và kiểm tra xem trình duyệt đã tự động cập nhật lại chưa.
 * 
 * Hãy đổi các biến dưới đây thành `true` khi bạn hoàn thành từng bước:
 */

// === ĐÁNH DẤU HOÀN THÀNH Ở DƯỚI ĐÂY ===
const daTaoDuAn = true;
const daChayServer = true;
const daSuaAppJsx = true;

console.log("Trạng thái hoàn thành:", { daTaoDuAn, daChayServer, daSuaAppJsx });
